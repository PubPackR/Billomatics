% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_order_type.R
\name{classify_order_type}
\alias{classify_order_type}
\title{Classify Order Type (New vs. Recurring Business)}
\usage{
classify_order_type(df)
}
\arguments{
\item{df}{A dataframe containing contract data. Must include the columns:
\code{client_id}, \code{confirmation_number}, \code{order_date},
\code{service_start}, and \code{service_end}.}
}
\value{
The original dataframe with an additional column \code{order_type}.
}
\description{
This function adds a column \code{order_type} to the provided dataframe.
It classifies contracts based on the gap between the previous contract's
service end date and the current order date.
}
\details{
A contract is classified as \strong{"Erstauftrag"} (New Business) if:
\enumerate{
\item The client has no previous contracts within the dataset.
\item The gap between the previous contract's \code{service_end} and the
current \code{order_date} is greater than 24 months (indicating no revenue
in the preceding 24 months).
}
Otherwise, it is classified as \strong{"Folgeauftrag"} (Recurring Business).
}
