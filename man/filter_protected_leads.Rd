% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crm_helpers.R
\name{filter_protected_leads}
\alias{filter_protected_leads}
\title{Filter out protected test leads from API mutations}
\usage{
filter_protected_leads(df)
}
\arguments{
\item{df}{dataframe with at least an \code{attachable_id} column; if
\code{attachable_type} is also present, only \code{people} rows are filtered}
}
\value{
filtered dataframe, or NULL if no rows remain
}
\description{
Removes rows targeting protected CRM lead IDs (people only) so that no
add/remove/update API call is sent for them. Returns NULL if the filter
empties the dataframe (callers should \code{return(invisible(NULL))} in that case).
}
\details{
Wired into the following mutating CRM API functions:
\itemize{
\item add_crm_tag, remove_crm_tag
\item add_crm_custom_fields, remove_crm_custom_fields, update_crm_custom_fields
(manage_crm_custom_fields is covered transitively via the three above)
\item add_crm_addresses, remove_crm_addresses
\item add_contact_details, remove_contact_details
\item update_crm_person, delete_crm_person
\item update_crm_position, create_crm_position
}

NOT PROTECTED (no attachable_id available — caller must filter upstream):
\itemize{
\item mark_crm_task_finished (operates on task_id)
\item move_crm_protocols, delete_crm_protocols (operate on protocol_id;
also affected by a CRM vendor bug — see crm_api_protocols.R)
}

OUT OF SCOPE (different ID space — companies, not people):
\itemize{
\item create_crm_company, update_crm_company
\item create_crm_person (no attachable_id; creates a new record)
}
}
\keyword{internal}
