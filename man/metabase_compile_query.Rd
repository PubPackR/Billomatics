% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metabase_api.R
\name{metabase_compile_query}
\alias{metabase_compile_query}
\title{Eine MBQL-Abfrage von Metabase nach SQL kompilieren lassen}
\usage{
metabase_compile_query(
  dataset_query,
  api_key,
  base_url = "https://metabase.studyflix.info",
  timeout_s = 30,
  max_retries = 3
)
}
\arguments{
\item{dataset_query}{Die Query-Definition der Card, so wie Metabase sie in
\code{card$dataset_query} liefert (Liste, kein JSON-String).}

\item{api_key}{Metabase-API-Key.}

\item{base_url}{Basis-URL der Metabase-Instanz.}

\item{timeout_s}{Timeout in Sekunden PRO VERSUCH.}

\item{max_retries}{Maximale Anzahl zusaetzlicher Versuche bei transienten
Fehlern (429/5xx), nach dem ersten Versuch.}
}
\value{
Character-Skalar mit der kompilierten SQL. Bricht mit \code{stop()}
ab, wenn das Feld \code{query} in der Antwort fehlt, leer ist oder kein
Text ist (z.B. eine verschachtelte Liste durch eine API-Aenderung) -
damit im Fehlerfall sauber abgebrochen wird, statt still etwas Falsches
als SQL zurueckzugeben.
}
\description{
Metabase kann eine im grafischen Editor gebaute Frage serverseitig in die
SQL uebersetzen, die es selbst ausfuehren wuerde. Damit lassen sich auch
GUI-Fragen als SQL-Vorbild verwenden, statt sie als "ohne SQL" fuehren zu
muessen.
}
\details{
Fragen mit Parametern oder Template-Tags koennen fehlschlagen; der Aufrufer
entscheidet, ob das ein Abbruch ist.
}
