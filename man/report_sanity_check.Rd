% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sanity_check.R
\name{report_sanity_check}
\alias{report_sanity_check}
\title{report_sanity_check}
\usage{
report_sanity_check(
  check_passed,
  check_name,
  check_message,
  con,
  asana_api_token,
  asana_project_gid,
  context = list(),
  logger = NULL
)
}
\arguments{
\item{check_passed}{logical. Result of the sanity check (TRUE = OK, FALSE = failed).}

\item{check_name}{character. Unique identifier for the check
(e.g. \code{"revenue_rows_not_empty"}). Used as primary key in DB.}

\item{check_message}{character. Human-readable description of the failure.}

\item{con}{A DBI database connection.}

\item{asana_api_token}{character. Asana Personal Access Token.}

\item{asana_project_gid}{character. GID of the target Asana project
(e.g. the "Interne Prozesse" project).}

\item{context}{list. Optional named list of additional metadata
(e.g. \code{list(rows = 0, table = "processed.revenue")}). Stored as JSONB.}

\item{logger}{A log4r logger object. If NULL, messages are written via \code{message()}.}
}
\value{
\code{invisible(check_passed)}
}
\description{
Reports a failed sanity check to the database and creates an Asana ticket
if the error is new or the previous ticket has been closed.
}
\details{
Only active in non-interactive mode (i.e., on the server). When called
locally (interactive session), the function returns silently without action.

The sanity check itself must be performed outside this function.
Pass its boolean result as \code{check_passed}.
}
